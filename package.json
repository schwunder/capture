{
  "$schema": "https://www.raycast.com/schemas/extension.json",
  "name": "capture",
  "title": "Capture",
  "description": "it captures context",
  "icon": "extension-icon.png",
  "author": "schwunder",
  "license": "MIT",
  "preferences": [
    {
      "name": "directory",
      "title": "Clip Directory",
      "description": "Directory where clippings are stored",
      "type": "directory",
      "default": "Documents/clip",
      "required": false
    },
    {
      "name": "isContent",
      "title": "Capture Content",
      "description": "Should the tab's content be captured as Markdown?",
      "type": "checkbox",
      "label": "Capture page content",
      "required": false,
      "default": false
    },
    {
      "name": "isScreenshot",
      "title": "Capture Screenshot",
      "description": "Should a screenshot be taken?",
      "type": "checkbox",
      "label": "Take a screenshot",
      "required": false,
      "default": true
    }
  ],
  "commands": [
    {
      "name": "index",
      "title": "Clip Page",
      "subtitle": "Capture Browser Context",
      "description": "Captures the active browser tab's title, URL, and optionally content and a screenshot.",
      "mode": "view"
    },
    {
      "name": "instant",
      "title": "Instant Clip",
      "subtitle": "Capture Browser Context",
      "description": "Instantly captures the active browser tab based on your preferences.",
      "mode": "no-view"
    }
  ],
  "dependencies": {
    "@raycast/api": "^1.102.6",
    "@raycast/utils": "^1.17.0"
  },
  "devDependencies": {
    "@raycast/eslint-config": "^2.0.4",
    "@types/node": "22.13.10",
    "@types/react": "19.0.10",
    "eslint": "^9.22.0",
    "prettier": "^3.5.3",
    "typescript": "^5.8.2"
  },
  "scripts": {
    "build": "ray build",
    "dev": "ray develop",
    "fix-lint": "ray lint --fix",
    "lint": "ray lint",
    "prepublishOnly": "echo \"\\n\\nIt seems like you are trying to publish the Raycast extension to npm.\\n\\nIf you did intend to publish it to npm, remove the \\`prepublishOnly\\` script and rerun \\`npm publish\\` again.\\nIf you wanted to publish it to the Raycast Store instead, use \\`npm run publish\\` instead.\\n\\n\" && exit 1",
    "publish": "npx @raycast/api@latest publish"
  }
}